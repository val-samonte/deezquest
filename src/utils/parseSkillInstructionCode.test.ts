import { describe, expect, test } from '@jest/globals'
import { parseSkillInstructionCode } from './parseSkillInstructionCode'

describe('parseSkillInstructionCode', () => {
  test('should passs', () => {
    expect(true)
  })
})
